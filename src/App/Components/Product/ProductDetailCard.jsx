import React from 'react'

const ProductDetailCard = (props) => {
    const {
      _id,
      avg,
      description,
      discount,
      feedbacks,
      image,
      name,
      price,
      qty,
      sku_code,
      slug,
      specification,
      status,
      thumbnail,
      thumbnail_hover,
    } = props;
  return (
    <div>ProductDetailCard</div>
  )
}

export default ProductDetailCard